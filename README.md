# appdome_mitm_checker
Used for Appdome Trials. Reads a list of hostnames, determines the network owner (e.g, CDN), and performs TLS/SSL validation tests. Failed tests would trigger an Appdome MiTM warning.
